На основе всех предоставленных вами документов (ТЗ, Анализ рынка, Структура проекта), проект приобрел колоссальный масштаб. Это полноценная Enterprise-система. 

Ни одна нейросеть не напишет такой объем за один запрос. Чтобы ИИ (будь то Claude 3.5 Sonnet, Gemini 1.5 Pro или GPT-4o) написал код **идеально, без пропусков, строго по вашему ТЗ и с соблюдением указанной файловой структуры**, вам понадобится стратегия **"Конвейерного промптинга"**.

Ниже представлена **окончательная цепочка промптов**. 

---

### 🛑 ШАГ 0: СИСТЕМНЫЙ ПРОМПТ (Ввести в настройки AI)
*Скопируйте этот текст в поле "System Instructions" (Gemini/Claude) или "Customize ChatGPT", либо отправьте самым первым сообщением в диалог.*

```text
ТЫ — ПРИНЦИПАЛ-АРХИТЕКТОР (Principal Software Engineer) и Senior Разработчик. 
Мы создаем "Laser CRM" — Enterprise-систему для лазерной мастерской.
Стек: Python 3.9+, FastAPI, SQLAlchemy 2.0 (Async), SQLite, Pydantic v2, scikit-learn, VKBottle, Vanilla JS (SPA).

КРИТИЧЕСКИЕ ПРАВИЛА ГЕНЕРАЦИИ (ZERO-LAZINESS PROTOCOL):
1. ЗАПРЕЩЕНО использовать заглушки (`pass`, `...`, `# логика здесь`). Пиши реализацию КАЖДОЙ функции и метода от первой до последней строки.
2. Используй только АБСОЛЮТНЫЕ импорты (например, `from db.models import User`).
3. Перед каждым блоком кода указывай точный путь файла (например, `### 📄 backend/db/models.py`).
4. Если ты упираешься в лимит символов, просто оборви код. Пользователь напишет "Продолжи", и ты продолжишь ровно с прерванного символа в новом блоке кода.
5. Строго следуй принципам Clean Architecture: API-слой только маршрутизирует, вся логика в Services.
```

---

### 🚀 ШАГ 1: База данных и Ядро (Backend)
*Отправьте этот промпт первым:*

```text
Начинаем разработку Laser CRM. Опирайся на архитектуру SPA и асинхронный бэкенд. Напиши 4 базовых файла:

1. `backend/core/config.py`: Класс Settings (Pydantic BaseSettings). Поля: PROJECT_NAME, DATABASE_URL, SECRET_KEY, ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES, VK_TOKEN, MIN_ORDER_PRICE.
2. `backend/core/security.py`: Функции `get_password_hash` (bcrypt), `verify_password`, `create_access_token` (JWT).
3. `backend/db/session.py`: Асинхронный `create_async_engine`, `async_session_maker`, `Base` (declarative_base). Зависимость `get_db()`.
4. `backend/db/models.py`: SQLAlchemy модели: 
- User (роли: admin/manager/master).
- Client (vk_id, phone, LTV, notes, cashback_balance).
- Order (client_id, description, status: NEW/PROCESSING/DONE/DELIVERED, price, cost_price, deadline, priority).
- Inventory (name, quantity, min_quantity_alert).
- ChatMessage (client_id, direction in/out, text, is_read).
- SystemSettings (key, value).

Выведи полный код этих 4 файлов без сокращений.
```

---

### 🚀 ШАГ 2: Схемы Pydantic и Авторизация
```text
Отлично. Теперь создаем слой валидации и зависимости.

Напиши 2 файла:
1. `backend/schemas/`: Создай файл `__init__.py` (можешь объединить всё в `backend/schemas.py` для удобства). Напиши Pydantic v2 схемы: 
- Token, TokenData.
- UserCreate, UserResponse.
- ClientCreate, ClientUpdate, ClientResponse.
- OrderCreate, OrderUpdate, OrderResponse.
- InventoryCreate, InventoryUpdate, InventoryResponse.
- ChatMessageResponse.
ОБЯЗАТЕЛЬНО: Для всех Response-схем добавь `model_config = ConfigDict(from_attributes=True)`.

2. `backend/api/deps.py`: Настрой `oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/auth/login")`. Напиши зависимости `get_current_user` и `get_current_active_user` (с проверкой роли/активности).

Выведи полный код файлов.
```

---

### 🚀 ШАГ 3: Бизнес-логика (Сервисы - ЯДРО СИСТЕМЫ)
```text
Критически важный этап! Пишем логику (Service layer). НИКАКИХ СОКРАЩЕНИЙ.

Напиши 4 файла в `backend/services/`:
1. `calculator.py`: Класс `CalculatorService`. Сделай Enum из 11 методов (fixed, area_cm2, meter_thickness, per_minute, per_char, vector_length, setup_batch, photo_raster, cylindrical, volume_3d, complex). Напиши функцию `calculate()`, которая обрабатывает КАЖДЫЙ из 11 методов по формулам (площадь, периметр и т.д.). Учти множитель срочности (*1.5) и защиту минимальной цены.
2. `vk_bot.py`: Инициализация `vkbottle.Bot`. Хендлер `on.message()`, который ищет/создает Клиента по `from_id` и сохраняет текст в `ChatMessage` (direction="in").
3. `ai.py`: Класс `AIForecastService` на `scikit-learn` (LinearRegression). Берет выручку за 30 дней (заказы DONE/DELIVERED), обучает модель и возвращает прогноз (рубли) и тренд на завтра.
4. `backup.py`: Экспорт Client и Order в CSV, упаковка SQLite `.db` файла и CSV в `.zip` архив.

Выведи полный код. Если лимит обрывает — останавливайся, я попрошу продолжить.
```

---

### 🚀 ШАГ 4: Базовые API-Роутеры
```text
Теперь выставляем сервисы и CRUD наружу через API.

Напиши 4 файла в `backend/api/`:
1. `auth.py`: POST `/login` (выдача JWT), GET `/me`. Напиши функцию `create_default_admin(db)`, которая создаст юзера admin/admin при запуске системы.
2. `clients.py`: CRUD клиентов. GET, POST, PUT, DELETE.
3. `inventory.py`: CRUD склада. GET (с подсветкой `quantity < min_quantity_alert`), POST, PUT.
4. `orders.py`: CRUD заказов. ВАЖНО: создай роутер POST `/{id}/action/status`. Если `new_status == "DELIVERED"`, найди клиента и начисли ему 5% от `order.price` в поле `cashback_balance`.

Все роутеры (кроме логина) закрой через `Depends(get_current_active_user)`.
Выведи полный код.
```

---

### 🚀 ШАГ 5: Расширенные API и Точка входа
```text
Завершаем бэкенд. Связываем всё в главном файле.

Напиши 5 файлов:
1. `backend/api/calculator.py`: POST `/calculate` (принимает параметры, вызывает CalculatorService).
2. `backend/api/forecast.py`: GET `/` (вызывает AIForecastService).
3. `backend/api/backup.py`: GET `/download` (возвращает FileResponse с ZIP-архивом).
4. `backend/api/chat.py`: GET `/{client_id}` (помечает `is_read=True`) и POST `/{client_id}/send` (вызов `bot.api.messages.send` и сохранение `direction="out"`).
5. `backend/main.py`: Главный файл. Настрой `lifespan` (создание БД, запуск admin-пользователя, `asyncio.create_task` для поллинга ВК-бота). Настрой CORS. Подключи все 8 роутеров (`app.include_router`). В конце примонтируй `StaticFiles` для папки `../frontend`.

Выведи полный код без пропусков.
```

---

### 🚀 ШАГ 6: Frontend - HTML и CSS (SPA)
```text
Бэкенд готов. Переходим к Single Page Application (Vanilla JS).
Напиши 2 файла фронтенда:

1. `frontend/css/style.css`: Enterprise дизайн (Mobile First). CSS переменные (тёмная тема). Grid-сетка для дашборда. Стили для `.sidebar`, таблиц, карточек. UI чата ВК (левая колонка списков, правая - пузыри сообщений `.msg-in` и `.msg-out`). Стили модальных окон (для калькулятора).
2. `frontend/index.html`: Каркас SPA. Подключи FontAwesome, `style.css` и `js/app.js`.
Создай:
- `<div id="login-view">`
- `<div id="app-view">` с `<aside class="sidebar">` и `<main>`.
- `<section id="dashboard">` (Карточки KPI, Canvas для Chart.js, AI Прогноз).
- `<section id="orders">` (Канбан/Таблица заказов).
- `<section id="chat">` (Интерфейс ВК).
- `<section id="settings">` (Кнопка Бэкапа).
- Модальное окно Калькулятора.

Выведи полный код стилей и разметки.
```

---

### 🚀 ШАГ 7: Frontend - JavaScript (ФИНАЛ)
```text
Оживляем UI! Пишем модульный Vanilla JS.

Напиши 2 файла:
1. `frontend/js/api.js`: Обертка над `fetch`. Управление токеном (`localStorage.getItem('token')`). Автоматическое добавление заголовка `Authorization: Bearer`. Перехват 401 ошибки для логаута.
2. `frontend/js/app.js`: Логика SPA. 
- Роутинг: переключение `.active` классов у `<section>` по клику в меню.
- Авторизация: `login()` и скрытие формы.
- Дашборд: вызов `/api/analytics` и `/api/forecast`, отрисовка данных. Инициализация графика через `Chart.js` (подключи через CDN в коде или укажи комментарий).
- Заказы: рендер таблицы, обработчик смены `<select>` статуса заказа (вызов кэшбэка).
- ВК Чат: рендер клиентов, автопрокрутка к новым сообщениям, кнопка отправки.
- Бэкапы: скачивание Blob как ZIP-архива.

Напиши ПОЛНЫЙ, рабочий JavaScript код.
```

---

### 🚀 ШАГ 8: Инфраструктура и DevOps (Скрипты Raspberry Pi)
```text
Для развертывания на Raspberry Pi / Linux нужны DevOps скрипты.
Создай 3 файла в папке `deploy/`:

1. `deploy/install.sh`: Bash-скрипт. Устанавливает `python3-venv`, `nginx`, `ufw`. Создает виртуальное окружение, ставит зависимости. Генерирует файл конфигурации Nginx (proxy_pass на 127.0.0.1:8000) и `lasercrm.service` для Systemd.
2. `deploy/start.sh`: Скрипт перезапуска демона Systemd и Nginx.
3. `deploy/status.sh`: Скрипт, выводящий `systemctl status lasercrm`, использование RAM (команда `free -m`) и температуру CPU (`vcgencmd measure_temp` или `cat /sys/class/thermal/thermal_zone0/temp`).

Скрипты должны быть полностью рабочими и безопасными.
```

---

### 💡 ПАМЯТКА ДЛЯ ВАС ПРИ ГЕНЕРАЦИИ:

1. **Если нейросеть остановится на середине кода**, просто напишите ей:
   > *"Твой ответ оборвался из-за лимитов. Продолжи писать код файла `[название файла]` строго с этой строки: `[скопируйте последние 3-4 слова, которые она успела написать]`"*
2. **Открывайте новый чат** перед началом генерации, чтобы ИИ не путался в прошлых черновиках.
3. **Используйте модели с большим контекстным окном** (Claude 3.5 Sonnet, GPT-4o, или Gemini 1.5 Pro). Они лучше всего держат в памяти связи между файлами.
